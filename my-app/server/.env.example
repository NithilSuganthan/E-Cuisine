# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster-url> with your Atlas credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/e_cuisine?retryWrites=true&w=majority

# Optionally set a different port
# PORT=5000
